//
//  GitMartTrigger.swift
//  
//
//  Created by Zachary Shakked on 10/4/22.
//

import Foundation

/*
 An idea
 
enum GitMartTriggerType {
    case count(Int) // executes a fixed number of time

    case countByVersion(Int) // executes a fixed number of times, but resets on new app versions
    case countAfterDate(Int, Date) // executes a fixed number of times after a date has passed
    case countPerCalendarUnit(Int, Calendar.Component) // executes a fixed number of times in a rolling calendar period
    case untilConditionTrue(String) // provide the actionType which will be evaluated in the GitMart delegate
}
*/
 
